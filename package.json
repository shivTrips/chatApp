{
  "name": "chat-app",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x",
    "yarn": ">=1.22.0"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "start": "cd server && yarn start"
  }
} 